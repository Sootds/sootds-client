import React, { FunctionComponent, memo } from 'react';
import { SignIn } from '../containers';

// Component
const SignInPage: FunctionComponent = () => <SignIn />;

export default memo(SignInPage);
